<h1>All Categories</h1>

<% categories.forEach(element => { %>
    <div class="card">
        <div class="card-body">
            <h2 class="card-title"><%= element.name %> </h2>
        </div>
    </div>
<% }) %>